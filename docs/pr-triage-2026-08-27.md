# Open PR Inventory — asnowfix/home-automation

> **Provenance and caveats.** Produced by a read-only survey agent on 2026-08-27 against `main`
> @ `5f42f288`. The agent was killed by a spend limit shortly after completing the PR pass, so its
> companion worktree inventory does **not** exist — see #559. Every conflict count and CI state here
> is a **measurement taken at 07:32 CEST that day** and will drift; re-measure before acting on any
> individual number. Recommendations are the agent's, not decisions — the only one already known to
> be wrong is **#567 (`MERGE`)**, which a later measurement contradicts; see the note in its section.

Generated: 2026-08-27 07:32 CEST, against `main` @ 5f42f288, `gh pr list --state open --limit 50` (confirmed 16 PRs, matches the assigned list exactly, same numbers, no drift).

Budget: 60 minutes, read-only. No mutations performed (no commits, pushes, checkouts, merges, comments).

Conflict counts below are measured with `git merge-tree --write-tree origin/main origin/<headRef>`
(git 2.54, computes merge-base automatically) and counting `CONFLICT (...)` lines in the output —
this is the actual file-level conflict against **current `origin/main`**, which differs from GitHub's
`mergeStateStatus`/`mergeable` fields whenever a PR's declared base is not `main` (see #409 below).

## Summary table

| # | Title (truncated) | Age(d) | Still applies? | Conflicts vs origin/main | Recommendation |
|---|---|---|---|---|---|
| 567 | docs: derive test timeout from `cover` not `-race` | 0 | still applies | 0 | MERGE |
| 516 | test(pool-pump): red tests for #512 window resolution | 8 | still applies (conceptually), file drifted | 0 | PARK — rebase, then pair with a real fix |
| 513 | [WIP] fix(pool-pump): resolve run window every daily check (#512) | 9 | cannot tell without deeper reading | 1 | PARK — stacked on merged #510, needs rebase + finish |
| 511 | feat(pool-pump): forecast fetch via MQTT proxy (#465) | 9 | cannot tell without deeper reading | 2 | NEEDS-HUMAN — merge-policy check failing |
| 438 | docs: JS heap allocation guide + costed pool-pump proposal (#433) | 21 | still applies | 1 | PARK — rebase (1-file AGENTS.md conflict), then merge |
| 437 | fix(pool-pump): restore pump on water-supply clear (#436) | 21 | cannot tell without deeper reading | 3 | PARK — needs rebase onto current pool-pump.js, re-verify |
| 430 | feat(minifier): esbuild engine, opt-in top-level mangling | 22 | still applies, draft-by-design | 1 | PARK — CI build failing, needs on-device verification before merge |
| 427 | test(422): real-device integration test campaign + #421 baseline | 22 | still applies | 0 | PARK — rebase (BEHIND), coverage-delta check failing |
| 425 | chore(daemon): remove legacy SolarAutomation (#406) | 22 | cannot tell without deeper reading — explicitly self-gated | 3 | PARK — author's own text says do not merge until #421 lands |
| 409 | arch: dependency-injection pass (#362) | 29 | cannot tell without deeper reading | 23 (vs main) / 16 reported in #564 vs branch | NEEDS-HUMAN — on the golang-expert-review divergence, see #564 |
| 269 | docs(scripts): plan for feature-script bundling | 73 | still applies (spec only, no code) | 0 | PARK — docs-only plan, no implementation started |
| 267 | feat(minifier): ES5 target + extract prometheus-metrics | 73 | superseded — blocking issue #266 still open | 0 | PARK — explicitly draft until #266 resolved |
| 259 | chore(learning): apply modernize fixes | 74 | still applies (mechanical) | 0 | PARK — learning-branch convention: draft by design, never merge |
| 254 | feat: MyHome workflows in JS on daemon-side goja host | 76 | cannot tell without deeper reading | 7 | NEEDS-HUMAN — large feature, no CI configured on this branch |
| 224 | refactor(rooms): room-centric heating refactor | 108 | cannot tell without deeper reading | 3 | NEEDS-HUMAN — large refactor, CI failing, stale 108 days |
| 212 | refactor: migrate pkg/shelly to go-shellies | 113 | superseded by ongoing in-place work | 53 | CLOSE — oldest, most conflicted, own body says "still needed" work never done |

---

## #567 — docs: derive the test timeout from `cover`, not `-race`

- Author asnowfix, **ready** (not draft). Created 2026-08-27, updated same day (age 0 days).
- **What it does**: One-line doc fix to `CLAUDE.md`'s test-timeout rule — clarifies that the timeout must be derived from `make cover` (no `-race`, plus coverage instrumentation), which this repo's own history shows costs *more* wall-clock than `-race`, not from `-race` alone as #561 did. Touches only `CLAUDE.md`.
- **Issues referenced**: #554 (MERGED), #556 (MERGED), #558 (MERGED), #561 (MERGED), #552 (CLOSED). All closed/merged — this PR documents a residual gap in that closed campaign, not a live blocker.
- **Does it still apply?** Still applies — it's a same-day fix to a live rule in `CLAUDE.md`, no other PR touches this section.
- **Merge conflicts**: 0 files (`git merge-tree --write-tree origin/main origin/docs/timeout-rule-cover`).
- **CI state**: `build`, `build-pr-deb`, `advise`, `merge-policy` pass; `coverage-delta` was pending at last check; `auto-merge` skipping (repo auto-merges on green).
- **Recommendation**: `MERGE` — trivial, well-scoped, no conflicts, blocking issues already closed. Just let CI finish.
- ⚠️ **This recommendation is superseded.** A fresh successful `cover` measurement, extracted the same
  day from the very run this PR was waiting on (GHA `33041300536`), puts `internal/shelly/scripts` at
  **583.15 s under `cover`** against **592.565 s under `-race`** — i.e. `cover` is **faster**, the
  opposite of this PR's central premise. See the analysis posted on #567. Do **not** merge on this
  row.

---

## #516 — test(pool-pump): red tests for #512 — the run window is never resolved after a restart

- Author asnowfix, **draft**. Created 2026-08-18, updated 2026-08-20 (age 8 days).
- **What it does**: Adds one new test file (`internal/shelly/scripts/pool_pump_window_init_test.go`) exercising the exact defect in #512 — no production code touched, deliberately red-before-fix. Explicitly stage 1 of a test-first loop; #513 is meant to be the fix.
- **Issues referenced**: #512 (OPEN — the defect itself, not yet fixed), #509 (CLOSED, already-fixed prerequisite this branch assumes present).
- **Does it still apply?** #512 is still open, so the test's target defect is real; **but** `pool-pump.js` on `main` has had ~19 unrelated commits since this branch's point (runtime accounting, button-fuse, timer rework) — the test may still compile/pass against current main since it only reads state, unverified here.
- **Merge conflicts**: 0 files — the new test file doesn't collide with anything on main.
- **CI state**: `build` **fails** (10m35s), `advise`/`build-pr-deb`/`coverage-delta` skipped as a result; `update-pr-description` passes.
- **Recommendation**: `PARK` — needs a rebase onto current main and a fix for the failing `build` check before it's useful as a red-test gate; #512 itself needs an owner (#513 stalled at WIP).

---

## #513 — [WIP] fix(pool-pump): resolve the run window on every daily check, not only when it moves (#512)

- Author asnowfix, **draft**. Created 2026-08-17, updated 2026-08-20 (age 9 days).
- **What it does**: Makes `verifySchedules()`'s window-resolution retry every daily check instead of only when the schedule moves, fixing the bug where `filtration-hiver` sat idle a full day despite correct schedules. Touches `pool-pump.js`, its test, `pkg/shelly/script/{device_state,run}.go`, and a gen2 listener + test.
- **Explicitly stacked**: body says "based on `fix/509-pool-pump-second-schedule-list` (#510), not `main` … please merge #510 first." **#510 is already MERGED** — so the stated precondition is satisfied and this branch should be rebasable onto main directly now.
- **Issues referenced**: #512 (OPEN, the defect), #510 (MERGED — prerequisite met), #509 (CLOSED, root cause this stacks on), #476/#469/#480/#433/#441 (all CLOSED, general pool-pump churn context, not direct blockers).
- **Does it still apply?** Cannot tell without deeper reading — the underlying #512 defect is still open and unfixed on main, so the *need* is real, but the diff is WIP and marked as such by the author; whether the specific mechanism still matches current `pool-pump.js` (19+ commits of drift) needs a human read.
- **Merge conflicts**: 1 file — `internal/shelly/scripts/pool-pump.js`.
- **CI state**: `build`, `build-pr-deb`, `coverage-delta`, `update-pr-description` all pass (green as of 2026-08-20); `advise`/`auto-merge` skipped.
- **Recommendation**: `PARK` — rebase onto current main (only 1 conflicting file), finish the WIP, and land alongside #516's red tests; #512 is a real, live defect worth finishing.

---

## #511 — feat(pool-pump): migrate forecast fetch off-device onto the MQTT fetch-and-transform proxy (#465)

- Author asnowfix, **ready** (not draft). Created 2026-08-17, updated 2026-08-23 (age 9 days).
- **What it does**: Device-side half of #465 — replaces `pool-pump.js`'s on-device `HTTP.GET`+`JSON.parse` of Open-Meteo with a subscription to the daemon's fetch-and-transform proxy (`myhome/fetchproxy`, merged as PR #507), moving the heap-heavy parse off the Shelly device onto the daemon. Includes a degraded/daemon-optional fallback path.
- **Issues referenced**: #465 (OPEN — the umbrella feature), #507 (MERGED — the daemon-side proxy this depends on, so its prerequisite is satisfied), #433 (OPEN — heap pressure this is meant to relieve).
- **Does it still apply?** Cannot tell without deeper reading — its daemon-side dependency (#507) is merged so the plumbing exists, but `pool-pump.js` has had heavy independent churn (button-fuse fix, runtime-accounting rewrite) since this branch's point, and the diff conflicts on the exact file being reworked.
- **Merge conflicts**: 2 files — `internal/shelly/scripts/pool-pump.js`, `internal/shelly/scripts/pool_pump_test.go`.
- **CI state**: `build`, `build-pr-deb`, `coverage-delta`, `advise` all pass; **`merge-policy` fails**.
- **Recommendation**: `NEEDS-HUMAN` — the failing `merge-policy` check (not just a stale build) plus 2-file conflict on the most actively-changed script in the repo means this needs a maintainer decision on rebase order relative to the other pool-pump PRs (#437, #513) before more effort goes in.

---

## #438 — docs: JS heap allocation guide + costed pool-pump proposal (#433)

- Author asnowfix, **ready** (not draft). Created 2026-08-06, updated 2026-08-20 (age 21 days).
- **What it does**: Documentation only, no code — adds a 759-line reusable JS-heap-reduction guide (`docs/shelly-heap-allocation.md`) and a 440-line costed, prioritized change list for fitting `pool-pump.js` into the Pro1 heap with solar enabled (`docs/433-pool-pump-heap-proposal.md`), plus an `AGENTS.md` pointer.
- **Issues referenced**: #433 (OPEN — the heap-pressure bug this proposal targets), #421 (OPEN), #405/#430 mentioned as context.
- **Does it still apply?** Still applies — it's a proposal/guide, not an implementation; #433 remains open and unaddressed, so the guidance is still needed. Nothing on main has superseded this analysis.
- **Merge conflicts**: 1 file — `AGENTS.md` (a pointer-line collision, trivial to resolve).
- **CI state**: `build`, `build-pr-deb`, `coverage-delta`, `advise` all pass; **`merge-policy` fails**.
- **Recommendation**: `PARK` — rebase the single-file `AGENTS.md` conflict and resolve whatever `merge-policy` is objecting to (likely branch-age or missing linked-issue policy check), then merge; this is low-risk, documentation-only, and unblocks #433 work.

---

## #437 — fix(pool-pump): restore pump on water-supply clear by re-deriving the schedule window (#436)

- Author asnowfix, **draft**. Created 2026-08-05, updated 2026-08-20 (age 21 days).
- **What it does**: Fixes a real, observed production defect — after a water-supply-protection event and a script restart, the pump was never restored because `STATE.savedOutput` was captured as `-1` (already off) at save time. Re-derives "should I be running now" from the schedule instead of remembering a stale value. Touches `pool-pump.js`, its test, and two `pkg/shelly/script` files.
- **Issues referenced**: #436 (OPEN — the defect, evidenced by 24 `pool.water_supply_protected` / 14 `pool.water_supply_restored` production event rows).
- **Does it still apply?** Cannot tell without deeper reading — #436 is still open (unfixed on main) so the need is real, but `pool-pump.js` has ~19 unrelated commits since this branch point and the fix touches the same water-supply/output-restore machinery that #549 ("a physical button press must never be refused by the anti-cycling fuse") and #550 (runtime accounting from observed transitions) also touched — high chance of logical, not just textual, overlap.
- **Merge conflicts**: 3 files — `pool-pump.js`, `pool_pump_test.go`, `pkg/shelly/script/device_state.go`.
- **CI state**: `build`, `build-pr-deb`, `coverage-delta`, `update-pr-description` all pass (green as of 2026-08-20).
- **Recommendation**: `PARK` — real, still-open bug with a passing-at-the-time fix, but needs a rebase and a re-read against #549/#550's later changes to the same restore/reconcile logic before trusting the diff.

---

## #430 — feat(minifier): esbuild engine with opt-in top-level mangling and symbol map

- Author asnowfix, **draft** (explicitly, on purpose — "stays draft until someone deliberately device-tests it," repo auto-merges green PRs). Created 2026-08-05, updated 2026-08-20 (age 22 days).
- **What it does**: Adds `esbuild` as a second, opt-in JS minifier alongside `tdewolff`, with top-level identifier mangling (tdewolff only mangles locals) and a symbol map to decode crash traces. Measured heap wins are substantial: `pool-pump.js` 36155B (tdewolff) → 31010B (esbuild+mangle); `garden.js` 23971B → 19493B.
- **Issues referenced**: #421 (OPEN, "Too many calls in progress" / OOM crash this aims to give headroom against), #426 (CLOSED — a different, already-shipped fix for the same crash), #367 (OPEN), #266 (OPEN, unrelated minifier-recursion bug).
- **Does it still apply?** Still applies as a feature — nothing on main adds a second minifier engine or top-level mangling; the measured heap savings are still relevant to the still-open #421/#433 heap-pressure issues.
- **Merge conflicts**: 1 file — `internal/myhome/shelly/script/ops.go`.
- **CI state**: `build` **fails** (6m42s), cascading skips for `build-pr-deb`/`coverage-delta`.
- **Recommendation**: `PARK` — fix the failing build (large diff, 23 files, adds a new dependency `evanw/esbuild`), then it still needs the on-device mangling verification the author flagged as a hard precondition before ever leaving draft.

---

## #427 — test(422): real-device integration test campaign + #421 baseline

- Author asnowfix, **ready** (not draft). Created 2026-08-04, updated 2026-08-20 (age 22 days).
- **What it does**: Adds `docs/integration-tests.md`, a living log of real-hardware test cases run against a spare Shelly Plus 1, using the template #422 specifies. First entry documents live reproduction of the "Too many calls in progress" crash at a third call site. Docs-only.
- **Issues referenced**: #422 (OPEN — the campaign framework), #421 (OPEN — the crash being documented), #1 appears to be a stray markdown numbering artifact ("Test case #1"), not an issue reference.
- **Does it still apply?** Still applies — #422's campaign framework and #421's crash are both still open; this is a living document meant to accumulate, not a one-shot patch that main could have overtaken.
- **Merge conflicts**: 0 files.
- **CI state**: `build`, `build-pr-deb`, `advise` pass; **`coverage-delta` fails** and **`merge-policy` fails** (both stale, from a 2026-08-04 run — the checks predate several later required-check additions).
- **Recommendation**: `PARK` — simple rebase (0 conflicts, `BEHIND` only) should re-trigger CI cleanly; worth landing since it's a genuinely useful, low-risk living doc and the failing checks look like staleness rather than real regressions.

---

## #425 — chore(daemon): remove legacy SolarAutomation state machine and pool.solar.* config (#406)

- Author asnowfix, **draft**, self-gated. Created 2026-08-04, updated 2026-08-20 (age 22 days).
- **What it does**: Deletes the daemon-side legacy `SolarAutomation` state machine, `pool_runtime_tracker.go`, and their tests, now that solar-driven pump control moved onto the device itself (#402/#403/#405). Final cleanup step of the #401 solar campaign.
- **Self-stated gate, in the PR body itself**: *"DRAFT — gated. #406 is explicitly gated in #401 on #402/#403/#405 being verified working against the real pool pump device, not just CI green... do not merge until #421 is fixed and the on-device verification actually passes."*
- **Issues referenced**: #406 (OPEN — this PR closes it), #401 (OPEN — the umbrella solar campaign, still active per #564), #402/#403/#405 (all CLOSED — the prerequisites this PR assumes shipped), #421 (OPEN — the explicit merge gate).
- **Does it still apply?** Cannot tell without deeper reading of the current #401 campaign status — #421 is still open, so by the author's own stated gate this must not merge yet; whether the deletion diff itself still matches current `daemon.go`/`run.go` needs checking given the conflicts found.
- **Merge conflicts**: 3 files, all `modify/delete` conflicts — `myhome/daemon/pool_runtime_tracker_test.go`, `solar_automation_test.go`, `solar_notice_test.go` (main modified these test files after this branch deleted them — a strong "diverged in both directions" signal).
- **CI state**: `build`, `build-pr-deb`, `coverage-delta`, `update-pr-description` all pass (green as of 2026-08-04, `advise`/`auto-merge` skipped).
- **Recommendation**: `PARK` — the author's own gate (#421 must land first) is still unmet; the 3 modify/delete conflicts on test files that main has since touched mean even a rebase attempt will need care to see whether main's edits to those tests are still relevant to delete.

---

## #409 — arch: dependency-injection pass — remove package-level singletons (#362)

- Author asnowfix, **ready** (not draft). Created 2026-07-28, updated 2026-08-26 (age 29 days — updated yesterday, actively worked).
- **What it does**: Six-commit pass removing package-level mutable singletons (`panicOnBugs`, the RPC method registry, `myhome.TheClient` across ~67 files) in favor of explicit constructor/parameter injection — plain Go, no DI framework, per the repo's simplicity rule. Touches ~97 files across nearly every `myhome/ctl/*` command plus core RPC/client plumbing.
- **On the `worktree-feature+golang-expert-review` divergence (per #564 — read in full for this task)**: **yes, directly on it.** `baseRefName` is literally `worktree-feature+golang-expert-review`, not `main`. Issue #564 names #409 explicitly as "the open PR on that branch," the last item of umbrella #368's Wave 3, and states a trial merge of `main` into that branch produced **16 conflicting files** as of 2026-08-26, including `go.work`, both Makefiles, `.goreleaser.yml`, the package-release workflow, `CLAUDE.md`, `internal/myhome/methods.go`, `pkg/shelly/script/{main,run}.go`, `pkg/beem/*`. #564 records the maintainer's plan: finish the current solar campaign (#401) first, cut `v0.12.1`, **then** merge `main` into this branch and "resurrect... to kill the divergence" — explicitly not by continuing to add work to the branch.
- **Issues referenced**: #362 (OPEN — this PR closes it), #368 (OPEN — umbrella), #408 (OPEN — the RPC registry race this partly fixes), #361 (OPEN, related generics work), #372/#373/#374 (all CLOSED — data-race fixes this built on).
- **Does it still apply?** Cannot tell without deeper reading — the work is real and substantial (removes a genuine unsynchronized package-level map, a real latent race per #408), but it sits on a branch whose divergence from `main` is the subject of an active, unresolved maintainer decision (#564) about *how* to reconcile, not *whether*.
- **Merge conflicts**: measured two ways — (a) against its **declared base** `worktree-feature+golang-expert-review`: `git merge-tree` gives 0 conflicts (consistent with GH's own `mergeable: MERGEABLE`); (b) against **current `origin/main`** directly (what actually matters if this is ever re-targeted): **23 conflicting files**, larger than #564's 16-file count from a day earlier — confirms #564's warning that "the cost grows daily."
- **CI state**: `build`, `build-pr-deb`, `coverage-delta`, `lint`, `race`, `advise` all pass; **`merge-policy` fails**.
- **Recommendation**: `NEEDS-HUMAN` — this is exactly the situation #564 says should be decided "with the merge conflicts in front of us," which is now (23 files and growing). Do not merge onto its current base without deciding #564's step 3/4 first; do not close, since #362's underlying race is real. This PR's fate depends entirely on the maintainer's #564 sequencing decision, not on anything intrinsic to the diff.

---

## #269 — docs(scripts): plan for feature-script bundling

- Author asnowfix, **draft**. Created 2026-06-14, updated 2026-08-20 (age 73 days).
- **What it does**: Adds `docs/feature-script-bundling.md`, an executable implementation spec (framework, conversions for 4 scripts, version tracking, migration/coexistence guarantees, test plan, on-device checklist) for combining multiple Shelly JS scripts to work around the 3-scripts-per-device cap. Docs only, no code.
- **Issues referenced**: none directly cited by number in the body (the 3-script-cap problem is described inline, not tied to a filed issue).
- **Does it still apply?** Still applies — no implementation of script bundling exists on `main`; this remains an unstarted spec.
- **Merge conflicts**: 0 files (`BEHIND` only).
- **CI state**: `build` **fails** (1m49s, from the original 2026-06-14 run), `advise` skipping, `update-pr-description` passes.
- **Recommendation**: `PARK` — harmless docs-only plan, zero conflicts, but 73 days stale with a failing build check; worth a cheap rebase to re-run CI, but no one has picked up the implementation, so it stays parked until someone commits to Phase 0.

---

## #267 — feat(minifier): set ES5 target and extract prometheus-metrics script

- Author asnowfix, **draft**, explicitly blocked. Created 2026-06-14, updated 2026-08-20 (age 73 days).
- **What it does**: Two things extracted from crash investigation PR #268 — (1) sets `Version: 2009` on the tdewolff minifier so it never emits ES6+ syntax Espruino can't parse; (2) the standalone `prometheus-metrics.js` script itself (extracted from `watchdog.js` by #268).
- **Explicit blocker, in the PR body**: "Status: draft — side script still crashes... This PR stays draft until #266 is resolved."
- **Issues referenced**: #266 (**still OPEN** — "Too much recursion" crash when `prometheus-metrics.js` is minified; the exact blocker named in the PR body), #268 (MERGED — the parent PR this was extracted from).
- **Does it still apply?** Superseded is too strong — more precisely: **still blocked by its own stated precondition**, which has not been resolved in the ~73 days since. #268 (the parent) already merged, so the `prometheus-metrics.js` extraction itself may already be redundant if #268's merge included it — needs a file-content diff to confirm, not done here given the time budget.
- **Merge conflicts**: 0 files (`BEHIND` only).
- **CI state**: `build` **fails** (1m52s, original run), `advise` skipping.
- **Recommendation**: `PARK` — cannot merge per the author's own explicit gate (#266 still open); worth checking whether #268's merge already superseded the `prometheus-metrics.js` half, which would shrink this PR to just the one-line minifier version bump.

---

## #259 — chore(learning): apply modernize fixes (interface{} → any, strings.Builder)

- Author asnowfix, **draft**. Created 2026-06-13, updated 2026-08-20 (age 74 days).
- **What it does**: Mechanical Go modernization via the `golang.org/x/tools` `modernize` analyzer — 24× `interface{}` → `any` in `cmd/datacollector/main.go`, and one `string +=` loop → `strings.Builder` in `myhome/metrics/exporter.go`. No behavioral change claimed.
- **Issues referenced**: none — this is explicitly a learning exercise, not tied to a filed issue.
- **Repo convention override (per this task's instructions and the repo's own recorded convention)**: PRs from `worktree-improve-golang-learn` relate to the maintainer's self-paced Go learning plan. The PR body says outright: *"Part of a self-paced Go learning plan — exercise A1... This is a learning branch, not intended to merge — kept as a draft for reference/review."* The repo-wide memory record (`feedback_learning_branches_stay_draft.md`) independently confirms: rebase/fix but never merge without an explicit ask.
- **Does it still apply?** The mechanical change itself still applies (nothing on main has since modernized those two files), but applicability is beside the point given the convention below.
- **Merge conflicts**: 0 files (`BEHIND` only).
- **CI state**: `build`, `build-pr-deb`, `coverage-delta`, `update-pr-description` all pass.
- **Recommendation**: `PARK` — **per repo convention, this must not be recommended for merge regardless of its (currently clean, currently green) state.** It exists as a reference/review artifact of the maintainer's learning plan and stays draft by design.

---

## #254 — feat: MyHome workflows in JavaScript on a daemon-side goja script host

- Author asnowfix, **draft**. Created 2026-06-11, updated 2026-08-24 (age 76 days — updated 3 days ago, so it's not abandoned).
- **What it does**: A substantial new capability — MyHome workflows written in JS, executed by an embedded `goja` engine on the daemon; devices invoke daemon-hosted scripts over MQTT (`script.invoke`); communication/persistence/infra stay in Go. Introduces instance names for multi-daemon coexistence and a new `myhome/scripthost` module. ~28 files, including a new go.work module.
- **On the `worktree-feature+golang-expert-review` divergence (per #564)**: **no** — `baseRefName` is `main`, not the divergent branch, and #564's explicit list of PRs on that branch (#395, #392, #400, #389, #380, #378, #375, #409) does not include #254. This PR is an independent, unrelated feature branch that happens to also be long-lived and draft.
- **Issues referenced**: none found by number in the body (references its own design docs `docs/plan-js-workflows.md`, `docs/plan-shelly-emulator-apis.md` rather than a filed issue).
- **Does it still apply?** Cannot tell without deeper reading — this is a large, self-contained architectural feature (own go.work module, own engine, own KVS/persistence design); nothing on `main` appears to have built an equivalent daemon-side script host, but 76 days of drift on `pkg/shelly/script/*` (heavily touched by #409, #430, #511 above) means real logical conflict risk beyond the 7 files git flags.
- **Merge conflicts**: 7 files — `.gitignore`, `AGENTS.md`, `internal/myhome/const.go`, `internal/myhome/methods.go`, `myhome/ctl/options/options.go`, `myhome/daemon/daemon.go`, `myhome/daemon/run.go`.
- **CI state**: "no checks reported on the `worktree-feature+my-js-home` branch" — CI has apparently never run on this branch (or ran under a workflow definition since replaced).
- **Recommendation**: `NEEDS-HUMAN` — architecturally significant, still being touched (updated 3 days ago), zero CI history to judge health from, and 7-file conflict against a codebase area under heavy concurrent change (#409/#430/#511). Needs the maintainer's judgment on whether the JS-workflow direction is still wanted before investing in a rebase.

---

## #224 — refactor(rooms): room-centric heating refactor

- Author asnowfix, **draft**. Created 2026-05-10, updated 2026-08-20 (age 108 days).
- **What it does**: A 6-phase refactor making "room" the first-class unit for heating — sensors, doors/windows, heaters, iCal occupancy agenda all owned by room; renames `myhome/temperature/` → `myhome/rooms/`; adds new `electricity`, `weather`, `ical` packages; heater scripts get an autonomous 6-gate control loop. Also fixes #200 (a stale README port-label bug).
- **Issues referenced**: #200 (**CLOSED** — its one concrete fix has already landed, likely separately, since this PR never merged), #235 (OPEN, solar electricity provider — tangential future work mentioned), #236 (OPEN, ENEDIS Tempo pricer — same).
- **Does it still apply?** Cannot tell without deeper reading — this is a large architectural rename/refactor (`temperature` → `rooms`) touching daemon wiring, config, and DB tables; 108 days old with zero commits on `main` touching `myhome/rooms` or `myhome/temperature` since its branch point (checked — no hits), meaning nothing has superseded it in place, but also nothing has kept it current against 108 days of unrelated daemon changes.
- **Merge conflicts**: 3 files — `.coverage-min`, `myhome/daemon/daemon.go`, `myhome/daemon/run.go`.
- **CI state**: `build` **fails** (3m46s, from the original 2026-05-10 run).
- **Recommendation**: `NEEDS-HUMAN` — #200, the one issue it explicitly claims to fix, is already closed (independently, since this never merged) — worth checking git blame on the README line to see whether that fix already shipped separately, which would mean re-scoping this PR. At 108 days and this size (new packages, DB schema rename), a rebase is a multi-day undertaking; needs a maintainer call on whether the room-centric direction is still wanted before any agent effort goes in.

---

## #212 — refactor: migrate pkg/shelly to github.com/asnowfix/go-shellies

- Author asnowfix, **draft**. Created 2026-05-05, updated 2026-08-20 (age 113 days — oldest open PR).
- **What it does**: Extracts `pkg/shelly/` and `pkg/devices/` into a standalone `go-shellies` repo (with history preserved via `git filter-repo`), moving generic Shelly CLI subcommands there and keeping only MyHome-specific commands in this repo. Uses local-path `replace` directives pending a published version.
- **Own body flags unfinished prerequisites**: "What's still needed... Tag `go-shellies v0.1.0` and replace the local-path `replace` directives... Move deferred `follow` and generic `script` half... See PLAN.md (Phase 6/7/8)." These were never completed in the 113 days since.
- **Issues referenced**: none by number — tracked instead via `PLAN.md` and `docs/plan-migrate-pkg-shelly-v2.md`, both of which this PR itself introduces.
- **Does it still apply?** Superseded by drift, not by an equivalent shipped change — `git log` shows 34 commits on `main` touching `pkg/shelly`/`go.work` since this branch's merge-base, and the repo's package layout (per `CLAUDE.md`'s current "Three-Tier Layer Rule" and ~45-module `go.work`) has evolved substantially past what this 113-day-old snapshot assumes. No later PR/commit completed the go-shellies extraction, but the codebase it would apply to has moved too far for the diff to be trustworthy.
- **Merge conflicts**: 53 files — by far the largest of any open PR, including 20+ `modify/delete` conflicts (main has kept modifying files this branch deletes: `pkg/shelly/mqtt/client.go`, `pkg/shelly/script/main.go`, several `go.mod`/`go.sum` files, etc.).
- **CI state**: `build` **fails** (26s — fails fast, likely a compile error from the split), `update-pr-description` passes.
- **Recommendation**: `CLOSE` — oldest PR, largest conflict surface (53 files) of the entire backlog, author's own body lists unfinished prerequisites that were never picked up in 113 days, and 34 subsequent main commits have kept evolving the exact code this PR tries to extract. If the go-shellies extraction is still wanted, it should be re-scoped and re-cut from current main rather than rebased.

---

## Notes on method / things a fresh read should double-check

- All 16 PRs are authored by `asnowfix` (the maintainer) — none are third-party contributions, so "still applies" is purely a question of drift against the maintainer's own subsequent work, not external-contributor staleness.
- `pool-pump.js` is the single hottest file in the repo: #516, #513, #511, #437 (four of the sixteen open PRs) all touch it, and `main` has had ~19-20 commits against it since the oldest of those branches was cut. Any rebase effort on those four should be sequenced together, not independently, since they likely conflict with each other's intent as much as with `main`.
- Conflict counts here are computed against **current `origin/main`**, which is the right number for "what would it cost to land this today" — but note #409's declared base is *not* `main`, so its GitHub-reported `mergeable` (clean) and this report's main-relative count (23 files) are both correct, just answering different questions. Flagged explicitly in its section.

## INCOMPLETE — none. All 16 PRs covered.
