// A plain comment, not JSDoc.
/* Not JSDoc either -- single leading star. */
/** A JSDoc comment carrying no annotation at all. */
function plainFunction() {
  return 1;
}

var plainConfig = {
  // Also just a regular comment.
  a: 1
};
